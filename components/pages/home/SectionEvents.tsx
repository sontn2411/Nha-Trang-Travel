import { bgEvent } from '@/assets'

const SectionEvents = () => {
  return (
    <div
      className=' h-96 mt-4  bg-center'
      style={{ backgroundImage: `url(${bgEvent.src})` }}
    >
      event
    </div>
  )
}

export default SectionEvents
